name: Notify Discord on PR opened
on:
  pull_request:
    types: [opened, reopened, ready_for_review]
jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Post to Discord
        env:
          WEBHOOK: ${{ secrets.DISCORD_WEBHOOK_URL_PR }}
          PR_TITLE: ${{ github.event.pull_request.title }}
          PR_URL: ${{ github.event.pull_request.html_url }}
          PR_AUTHOR: ${{ github.event.pull_request.user.login }}
          REPO: ${{ github.repository }}
        run: |
          payload=$(jq -n \
            --arg repo "$REPO" \
            --arg title "$PR_TITLE" \
            --arg url "$PR_URL" \
            --arg author "$PR_AUTHOR" \
            '{
              content: "**PR opened! :)**<@&1453490417264754749><@&1453490988923224148>",
              embeds: [
                {
                  title: $title,
                  url: $url,
                  description: "**Repo:** \($repo)\n**Author:** \($author)",
                  color: 3066993
                }
              ]
            }')
          curl -sS -X POST -H "Content-Type: application/json" -d "$payload" "$WEBHOOK"